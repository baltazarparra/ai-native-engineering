---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionBlock from '../components/SectionBlock/SectionBlock.astro';
import Grid from '../components/Grid/Grid.astro';
import Button from '../components/Button/Button.astro';
import Card from '../components/Card/Card.astro';
import Badge from '../components/Badge/Badge.astro';
import Footer from '../components/Footer/Footer.astro';
import Discussion from '../components/Discussion/Discussion.astro';
import HomeFoundations from '../components/HomeFoundations/HomeFoundations.astro';
import HeroStickers from '../components/HeroStickers/HeroStickers';
import HeroGhostCursors from '../components/HeroGhostCursors/HeroGhostCursors';
import { getSessions } from '../data/sessions';
import {
  getHomeAlternateLinks,
  getSessionHref,
  getProjectHref,
  getSkillsHref,
} from '../lib/i18n';

const lang = 'pt-BR';
const sessions = getSessions(lang);

function levelToBadgeVariant(level: string) {
  const map = {
    beginner: 'yellow',
    intermediate: 'blue',
    advanced: 'green',
  } as const;
  return map[level as keyof typeof map] || 'default';
}

function levelToLabel(level: string) {
  const map = {
    beginner: 'iniciante',
    intermediate: 'intermediário',
    advanced: 'avançado',
  } as const;
  return map[level as keyof typeof map] || level;
}
---

<BaseLayout
  title="Engenharia de Software na Era da IA"
  description="Um guia prático pra quem quer usar IA de verdade no dia a dia de desenvolvimento, sem hype e sem medo."
  lang={lang}
  alternateLinks={getHomeAlternateLinks()}
>
  <main id="main-content">
    <!-- hero -->
    <SectionBlock as="header" padding="xl" maxWidth="lg">
      <div class="hero-layer">
        <HeroGhostCursors client:idle />
        <HeroStickers client:idle lang={lang} />
        <div class="hero-content">
          <Badge variant="green">100% gratuito e open source</Badge>
          <h1 class="hero-title">Engenharia de Software na Era da IA</h1>
          <p class="hero-tagline">
            Um guia prático pra quem quer usar IA de verdade no dia a dia de
            desenvolvimento, sem hype e sem medo.
          </p>
        </div>
      </div>
    </SectionBlock>

    <HomeFoundations lang={lang} />

    <!-- session cards -->
    <SectionBlock padding="xl" id="sessoes">
      <h2>Sessões</h2>
      <Grid columns={3} gap="lg" align="stretch">
        {
          sessions.map((session) => (
            <Card
              title={session.title}
              href={getSessionHref(lang, session.slug)}
              accent={session.accent}
            >
              <Fragment slot="meta">
                <Badge variant={levelToBadgeVariant(session.level)} size="sm">
                  {levelToLabel(session.level)}
                </Badge>
                <Badge size="sm">{session.readingTime} min</Badge>
              </Fragment>
              <p>{session.summary}</p>
              <Fragment slot="footer">
                <span class="session-order">Sessão {session.order}</span>
              </Fragment>
            </Card>
          ))
        }
      </Grid>
    </SectionBlock>

    <!-- projeto prático -->
    <SectionBlock padding="xl" alt>
      <div class="project-cta-block">
        <Badge variant="coral">Novo</Badge>
        <h2>Projeto Prático</h2>
        <p>
          Já viu a teoria? Agora crie um projeto do zero com seu agente de
          código e publique no GitHub Pages. Um guia prático que ensina o fluxo
          agêntico de verdade.
        </p>
        <Button href={getProjectHref(lang)} size="lg">
          Ir para o projeto prático
        </Button>
      </div>
    </SectionBlock>

    <!-- skills -->
    <SectionBlock padding="xl">
      <div class="skills-cta-block">
        <Badge variant="blue">Skills</Badge>
        <h2>Compartilhe processos com seus agentes</h2>
        <p>
          Entenda o que são skills, como elas ajudam times a manter boas
          práticas e instale uma quality gate com um comando NPX.
        </p>
        <Button href={getSkillsHref(lang)} size="lg"> Ver skills </Button>
      </div>
    </SectionBlock>

    <Discussion lang={lang} slug="home" title="Engenharia de Software na Era da IA" />

    <!-- footer -->
    <Footer lang={lang} />
  </main>
</BaseLayout>

<style>
  .hero-layer {
    position: relative;
  }

  .hero-content {
    position: relative;
    z-index: 3;
    isolation: isolate;
  }

  .hero-title {
    margin-top: var(--space-md);
  }

  .hero-tagline {
    font-size: var(--text-xl);
    color: var(--color-text-muted);
    max-width: 42ch;
    margin-block: var(--space-lg);
    line-height: var(--leading-relaxed);
  }

  .session-order {
    font-family: var(--font-heading);
    font-weight: var(--weight-medium);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .project-cta-block {
    text-align: center;
    max-width: 42ch;
    margin-inline: auto;
  }

  .project-cta-block h2 {
    margin-top: var(--space-sm);
  }

  .project-cta-block p {
    margin-block: var(--space-md) var(--space-lg);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
  }

  .skills-cta-block {
    border: var(--border-thick);
    border-radius: var(--radius-md);
    background-color: var(--color-bg-alt);
    box-shadow: var(--shadow-lg);
    padding: var(--space-xl);
    max-width: 760px;
    margin-inline: auto;
    text-align: center;
  }

  .skills-cta-block h2 {
    margin-top: var(--space-sm);
  }

  .skills-cta-block p {
    margin: var(--space-md) auto var(--space-lg);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
    max-width: 54ch;
  }

  h2 + p {
    margin-block: var(--space-md);
  }
</style>
