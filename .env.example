# Liveblocks public API key — enables the multiplayer hero stickers.
# Without this set, the site runs in local-only mode (no realtime sync).
# Get a key at https://liveblocks.io (it must start with `pk_`).
PUBLIC_LIVEBLOCKS_KEY=
