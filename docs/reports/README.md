# Implementation reports

Structured handoff documents generated when using the **`/report`** skill (see `.cursor/skills/report/SKILL.md`).

Files are named `YYYY-MM-DD-<short-slug>.md`. Commit them when you want history in the repo; otherwise keep them local.
